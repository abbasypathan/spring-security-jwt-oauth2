* Creating spring boot application

- We can create template of spring boot application https://start.spring.io/
- We can select build tool, java version, add dependency, package name, artifact id
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

* Spring Security

- To enable the security we just have to add security dependency, It will ask for the username and password, username will be user & password is printed in console randomly generated

<dependency>
	<groupId>org.springframework.boot</groupId>
	<artifactId>spring-boot-starter-security</artifactId>
</dependency>

- We can configured our own username & password

spring.security.user.name = ${SECURITY_USER_NAME:admin}
spring.security.user.password = ${SECURITY_USER_PASSWORD:admin}

- Spring security internal flow

User Entered Credentials
        |
      (1)
        v
+---------------------------+
| Spring Security Filters   |
+---------------------------+
   |        |          |
  (2)      (3)        (9)
   |        |          v
   v        v      +----------------+
+----------------+ | Security       |
| Authentication | | Context        |
+----------------+ +----------------+
        |
       (3)
        v
+-----------------------------+
| Authentication Manager      |
+-----------------------------+
        |
       (4)
        v
+----------------------------------+
| Authentication Providers         |
+----------------------------------+
     |                      |
    (5)                    (6)
     v                      v
+-------------------+   +------------------+
| UserDetails       |   | Password Encoder |
| Service/Manager   |   +------------------+
+-------------------+
        |
       (7)
        v
+-----------------------------+
| Authentication Manager      |
+-----------------------------+
        |
       (8)
        v
+---------------------------+
| Spring Security Filters   |
+---------------------------+
        |
       (10)
        v
User gets response (Authenticated / Failed)

-------------------------------------------------------

REQUEST
  |
  v
Filters (Security Filter Chain)
  |
  v
Authentication (if login)
  |
  v
SecurityContextHolder stores user
  |
  v
Authorization check (roles/permissions)
  |
  v
DispatcherServlet
  |
  v
Controller
  |
  v
RESPONSE

- Servlet container are web server/app server like tomcat, it convert http request into ServletRequest object
- App processes request returns ServletResponse and again servlet convert response into http format
- JSESSIONID as cookie created by spring security by default

========================================================================================================================================================================================================

** Changing default security configuration

- SecurityFilterChain is the object to configured our custom security

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.web.SecurityFilterChain;

import static org.springframework.security.config.Customizer.withDefaults;

@Configuration
public class ProjectSecurityConfig {

    @Bean
    SecurityFilterChain defaultSecurityFilterChain(HttpSecurity http) throws Exception {
	// It will require authentication for all request
        /*http.authorizeHttpRequests((requests) -> requests.anyRequest().authenticated());*/

	//It will permit all request without authentication
        /*http.authorizeHttpRequests((requests) -> requests.anyRequest().permitAll());*/

	//It will deny all request even if user is authenticated
        /*http.authorizeHttpRequests((requests) -> requests.anyRequest().denyAll());*/

	//Here few will be required authentication & few are not
        http.authorizeHttpRequests((requests) -> requests
                .requestMatchers("/myAccount", "/myBalance", "/myLoans", "/myCards").authenticated()
                .requestMatchers("/notices", "/contact", "/error").permitAll());

        //Default authentication login page where we can enter credentials
        http.formLogin(withDefaults());

        // It will diable form login authentication
        //http.formLogin(flc -> flc.disable());

        //Http basic authentication which will pass credential in basic 64 encoded form like Basic ADSGYUH876
        http.httpBasic(withDefaults());

        //It will disable http basic authentication
       // http.httpBasic(hbc-> hbc.disable());

        return http.build();
    }
}

- http.formLogin(withDefaults()); opens default login page which is showing on browser
- We can disable this by using : http.formLogin(flc -> flc.disable());
- Once it's disabled browser will open it's own popup to enter credentials and spring security will do the Basic authentication
- Credentials are converted into Basic username:password (In encoded format)

- It will disable form login authentication
http.formLogin(flc -> flc.disable());

- It will disable http basic authentication
http.httpBasic(hbc-> hbc.disable()); 

- If we disabled both then we will get 403 as no option to authenticate user

========================================================================================================================================================================================================

** Defining & Managing Users using InMemoryUserDetailsManager

- In operation config we can use only single user so using InMemoryUserDetailsManager we can create different users with different roles

@Bean
public UserDetailsService userDetailsService() {
    // If we try to use plain password it won't be able authorize ad spring by default this password is encoded
    // by using {noop} we can explicitly mentation to spring that use as plain text
    UserDetails user = User.withUsername("user").password("{noop}12345").authorities("read").build();
    UserDetails admin = User.withUsername("admin").password("{noop}admin").authorities("admin").build();
    return new InMemoryUserDetailsManager(user, admin);
}

- We can use password encoder to encode password
- https://bcrypt-generator.com/ generate crypt hash value from plan text

@Bean
public UserDetailsService userDetailsService() {
    // If we try to use plain password it won't be able to authorize ad spring by default this password is encoded
    // by using {noop} we can explicitly mentation to spring that use as plain text
    UserDetails user = User.withUsername("user").password("{noop}12345").authorities("read").build();
    UserDetails admin = User.withUsername("admin").password("{bcrypt}$2a$12$ZGsrSHxI87v/r5DDSuaKeuPbTOMFfAc5ILnkrTYIlU9Hv907qTUpy").authorities("admin").build();
    return new InMemoryUserDetailsManager(user, admin);
}

@Bean
public PasswordEncoder passwordEncoder() {
    // This is having multiple password encoder, we are using default encoder which is spring boot consider
    return PasswordEncoderFactories.createDelegatingPasswordEncoder();
}

- We have option to add password checker for general common passwords

//It will disallow user to use common password like password,1234 etc
@Bean
public CompromisedPasswordChecker compromisedPasswordChecker() {
    return new HaveIBeenPwnedRestApiPasswordChecker();
}

- UserDetailsService used for get user details by username
- UserDetailsManager have all options like create, update, change password, delete user

+------------------------------------------------------+
|              UserDetailsService                      |
|                    (Interface)                       |
+------------------------------------------------------+
                         |
                         v
 Core interface which loads user-specific data.

 Methods:
   ✓ loadUserByUsername(String username)
                         |
                         v
+------------------------------------------------------+
|               UserDetailsManager                     |
|                    (Interface)                       |
+------------------------------------------------------+

 Extension of UserDetailsService which provides:
   - create new users
   - update existing users
   - delete users
   - change password

 Methods:
   ✓ createUser(UserDetails user)
   ✓ updateUser(UserDetails user)
   ✓ deleteUser(String username)
   ✓ changePassword(String oldPwd, String newPwd)
   ✓ userExists(String username)

          |                    |                    |
          v                    v                    v
+----------------+   +----------------+   +----------------+
| InMemoryUser   |   | JdbcUser       |   | LdapUser       |
| DetailsManager |   | DetailsManager |   | DetailsManager |
+----------------+   +----------------+   +----------------+

 Sample implementation classes provided by Spring Security team.

- UserDetails is the interface which is having all user related data to authenticate
- UserDetails is the return type in all scenarios where we try to load the user information from storage systems.
  Used inside:
   - UserDetailsService
   - UserDetailsManager

+--------------------------------------------------+
|                 UserDetails                      |
|                  (Interface)                     |
+--------------------------------------------------+
                        |
                        v
+--------------------------------------------------+
|                     User                         |
|                    (Class)                       |
+--------------------------------------------------+

 Methods:
   ✓ getPassword()
   ✓ getUsername()
   ✓ getAuthorities()
   ✓ isAccountNonExpired()
   ✓ isAccountNonLocked()
   ✓ isEnabled()
   ✓ isCredentialsNonExpired()
   ✓ eraseCredentials()

- Authentication interface which also having same details & this is used to store in Security Context
- Authentication is the return type in all the scenarios where we are trying to determine if the authentication is successful or not. Like inside the AuthenticationProvider & AuthenticationManager

+--------------------------------------------------+
|                    Principal                     |
|                   (Interface)                    |
+--------------------------------------------------+
                        |
                        v
+--------------------------------------------------+
|                 Authentication                   |
|                   (Interface)                    |
+--------------------------------------------------+

 Methods:
   ✓ getName()
   ✓ getPrincipal()
   ✓ getAuthorities()
   ✓ getCredentials()
   ✓ getDetails()
   ✓ isAuthenticated()
   ✓ setAuthenticated()
   ✓ eraseCredentials()

                        |
                        v
+--------------------------------------------------+
|      UsernamePasswordAuthenticationToken         |
|                     (Class)                      |
+--------------------------------------------------+

========================================================================================================================================================================================================

** Defining & Managing Users using a database

1. Using same table schema which spring security uses by default

- Setup Docker & warp command line interface
- Setup MySQL database using docker
- Command : docker run -p 3306:3306 --name springsecurity -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=eazybank -d mysql
- -p : port(-p hostPort:containerPort), --name : is container name, -e : env variable, -d : detached mode no logs printed, mysql : docker image name which is already present in docker registry
- Connect to the database using any client (DBever)
- We need to add database configuration into properties file + need to add JDBC, MYSQL & Spring Data JPA dependency
- We need to update UserDetailsService bean creation logic as below

@Bean
public UserDetailsService userDetailsService(DataSource dataSource) {
    // It will connect to the database which we configured and fetch user details by username
    // It will force you to use same schema which JdbcUserDetailsManager uses
    return new JdbcUserDetailsManager(dataSource);
}
- Here we need to create same schema and tables names to store user credentials

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

2. Creating our own custom tables

- We can create JPA repository to same structure as our table schema to fetch user details
- Then we need to provide implementation of loadUserByUsername() method by extending UserDetailsService interface  
- In this we can fetch user by username/email and then create UserDetails object

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

3. Adding REST API for registering user
- We can use same repository to save user details in database
- We need to add new endpoint into permitAll() in authentication config class
- Even if we add new endpoint into config spring security won't allow to do it by default it will block DML operations (CSRF protection)
- We need to provide implementation or need to disable it
- We can disable using : http.csrf(csrfCOnfig -> csrfCOnfig.disable()) 


========================================================================================================================================================================================================

** Password Management with PasswordEncoders

- PasswordEncoder used to encrypt password and stored into DB

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
1. Encoding
- Encoding is defined as the process of converting data from one form to another and has nothing to do with cryptography.
- It involves no secret and completely reversible.
- Encoding can't be used for securing data. Below are the various publicly available algorithms used for encoding.
- Ex: ASCII, BASE64, UNICODE

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

2. Encryption
- Encryption is defined as the process of transforming data in such a way that guarantees confidentiality.
- To achieve confidentiality, encryption requires the use of a secret which, in cryptographic terms, we call a "key"
- Encryption can be reversible by, using decryption with the help of the "key". As long as the "key" is confidential, encryption, can be considered as secured.

A. Symmetric encryption
- In symmetric key encryption, the same key used to encrypt the data is used to decrypt the data. Some famous symmetric encryption algorithms include AES, known for its security and used globally; Triple DES, an enhancement of DES providing better security; and Blowfish, notable for its speed and variable key lengths. Each offers distinct advantages for protecting data. symmetric key encryption is generally used for encrypting data at rest.
- Data at rest means data are stored in someplace and used by single organization/application

B. Asymmetric encryption
- Asymmetric encryption functions like a unique lock with two distinct keys: one for locking (encrypting) and one for unlocking (decrypting). You keep the private key, which unlocks the message, secure with you, while freely distributing the public key to anyone who wants to send you an encrypted message. They use this public key to encrypt their message, and only you can decrypt it using your private key. This system ensures secure communication, as it relies on a pair of keys: a public key for encryption and a private key for decryption
- Asymmetric key encryption is ideal for encrypting data in in-transit, where you need to share the key with another system. The most commonly used asymmetric algorithms include Rivest-Shamir-Adleman (RSA), Diffie-Hellman, Elliptic Curve Cryptography (ECC) and Pretty Good Privacy (PGP).

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

3. Hashing (Use to stored credentials in DB)

- In hashing, data is converted to the hash value using some hashing function.
- Data once hashed is non-reversible. One cannot determine the original data from a hash value generated.
- Given some arbitrary data along with the output of a hashing algorithm, one can verify whether this data matches the original input data without needing to see the original data

A. Drawbacks
- Hash value will be same foe same password, if 2 user have same password hash value also same
- Brute force attack : Generating hash for common password and if hash value matches attacker easily can identify the password for user
- Dictionary or Rainbow attack : attacker will have common password hash value in table with recalculated values if hash value match password will be exposed
- Hashing is super fast it is easy for attacker to execute their logic to guess password

B. Overcome of Drawbacks
   a. Salt value during hashing password
   - While generating hash of password we can add random string to the password and then generate hash and store random string and hash into DB
   - With salt brut force still possible for that we can restrict user to use combination of password & keep 3 attempt after that we can lock accounts
   b. Intentionally slowing hashing function
   - Even if attacker have all db data with salt value if we slower the hashing function attacker needs lots of time to guess password 
   - PBKDF2, bcrypt, scrypt and Argon2.
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

4. PasswordEncoders

- Spring security PasswordEncoder
- This is the implementation of this interface PBKDF2, bcrypt, scrypt and Argon2.

public interface PasswordEncoder {
	String encode(CharSequence rawPassword);
	boolean matches(CharSequence rawPassword, String encodedPassword);
	
	default boolean upgradeEncoding(String encodedPassword) {
		return false;
	}
}

========================================================================================================================================================================================================

** Understanding Authentication Provider and Implementing it

- By default spring security uses DaoAuthenticationProvider which expects UserDetailsService/Manager 
- So default provider uses username & password for authentication if we have some custom requirement such as age validation or country authentication in that case we need to implement our own provider

public interface AuthenticationProvider {
	Authentication authenticate(Authentication authentication) throws AuthenticationException;
	boolean supports(Class<?> authentication);
}

- The authenticate method receives and returns authentication object. We can implement all our custom authentication logic inside authentication method.
- The second method in the AuthenticationProvider interface is supports(Class<?> authentication). You'll implement this method to return true if the current AuthenticationProvider supports the type of the Authentication object provided.

- To create our own provider we need to implements AuthenticationProvider interface
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

* Env specifically security configuration

- @Profile("!prod") - if not prod profile then it will be used
- @Profile("prod") - if prod profile is active then it will use marked class

========================================================================================================================================================================================================

** Spring Security customizations for most common use cases (Section7)

1. Accepting only HTTPS protocol

- Allow only http protocol http.redirectToHttps(https -> https.disable())
- Allow only https protocol http.redirectToHttps(Customizer.withDefaults())
- https://localhost:8443 with TLS configuration 

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

2. Exception handling in Spring Security

- 403(AccessDeniedHandler) -> user authenticate but no enough permission to access
- 401(AuthenticationEntryPoint) -> user not authorized 
- Inside Spring Security authentication & authorization related exceptions are handled by ExceptionTranslationFilter
- Need to pass object of custom exception into basic auth in SeurityConfig class http.httpBasic(hbc -> hbc.authenticationEntryPoint(new CustomBasicAuthEntryPoint()));
- Need to configured global exception for access denied error in SeurityConfig class http.exceptionHandling(ehc -> ehc.accessDeniedHandler(new CustomAccessDeniedHandler()));
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

3. Session timeout & session configuration

- By default session is 30 min timeout, where JSESSIONID is stored by spring security
- We can configured session server.servlet.session.timeout=${SESSION_TIMEOUT:2m}
- Default allowed time is greater than or equal to 2m
- We can redirect user to another page when session is invalid http.sessionManagement(session -> session.invalidSessionUrl("/invalidSession"))
- We can set number of session for an user http.sessionManagement(session -> session.invalidSessionUrl("/invalidSession").maximumSessions(1))
- Default multiple session are allowed
- It will create new session and previous will be invalidated(default behavior)
- We can keep first session and block 2nd session creation http.sessionManagement(session -> session.invalidSessionUrl("/invalidSession").maximumSessions(1).maxSessionsPreventsLogin(true))
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

4. Session Fixation, Session Hijacking Attacks

- 

========================================================================================================================================================================================================